{% extends 'base.html.twig' %}

{% block body %}
  <div class="col-12">
    <a href="{{
      path(
        'user_backoffice_user_read',
        {
          id: user.id
        }
      )
      }}">
      Retour
    </a>
    <h2>
      Modifier un magasin
    </h2>
  </div>
  {{ form_start(form) }}
  {{ form_row(form) }}
  <button class="btn btn-primary">Modifier</button>
  {{ form_end(form) }}
{% endblock %}
